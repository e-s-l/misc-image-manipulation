# About

## One Year On

This needs some significant work if it is to remain public.

## TODO

- use a package/module implemenation
- use proper arg parses
- add a quadrature image-manip algorithm, see *e.g.* https://github.com/fogleman/Quads/blob/master/main.py
- use standard python type hinting
-  use virtual enviroments and `pip freeze` to get the requirements file.
